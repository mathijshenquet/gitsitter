There is a git rebase in progress with conflicts. Resolve all conflicts and complete the rebase.

Be conservative — only resolve where intent is clear. For ambiguous conflicts, make your best effort but mark with a FIXME comment. Do not abort the rebase.
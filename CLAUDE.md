
This repo is using nix with a dev shell, see @flake.nix. It is likely that the devshell is already active, if not make sure you use it instead of eg the systems cargo.

# General

- never use planning or code exploration unless asked to
- never used agents unless asked to